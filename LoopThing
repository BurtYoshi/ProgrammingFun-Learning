import java.util.*;

public class LoopThing {
    public static void main(String[] args) {
        System.out.println("Which of the shapes below would you like to make");
        System.out.println("Triangle, Square, Circle(specification)");
        
        Scanner InputReader = new Scanner(System.in);
        int UserInputI = InputReader.nextInt();
        String UserInputS = InputReader.nextLine();
        
        if (UserInputS.equalsIgnoreCase("Triangle")) {
            System.out.println("How many rows should the triangle have?");
            UserInputI = InputReader.nextInt();
            int size = UserInputI;
            
            for (int row = 1; row <= size; row++) {
                for (int col = 1; col <= size; col++) {
                    
                }
            }
        }
    }
}
